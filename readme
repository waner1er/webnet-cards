# Webnet Cards - Jeu de Cartes

Application web de tirage et tri de cartes avec règles personnalisables.

---

## Get Started

### Prérequis

- **PHP** 8.2 ou supérieur
- **Composer** 2.x
- **Symfony CLI** (recommandé) ou serveur PHP

### Installation

```bash
# 1. Cloner le projet
git clone <repository-url>
cd webnet-cards

# 2. Installer les dépendances
composer install

# 3. Démarrer le serveur de développement
symfony server:start
# OU avec PHP natif
php -S localhost:8000 -t public

# 4. Accéder à l'application
# Ouvrir dans le navigateur : http://localhost:8000
```

### Test en ligne de commande

```bash
# Tirer 10 cartes en mode console
php bin/console game:test 10

# Tirer toutes les cartes (paquet complet)
php bin/console game:test 52
```

---

##  Utilisation

### Mode aléatoire (par défaut)

1. Saisir le nombre de cartes à tirer (1-52)
2. Cliquer sur **"🎲 Tirer les cartes"**
3. Consulter :
   - L'ordre aléatoire des couleurs généré
   - L'ordre aléatoire des valeurs généré
   - La main non triée (ordre de tirage)
   - La main triée (selon les règles aléatoires)

## Technologies

- **Backend** : Symfony 7.x, PHP 8.2+
- **Frontend** : Twig, MVP.css, JavaScript vanilla

## Fonctionnalités

-  Tirage aléatoire de 1 à 52 cartes
-  Génération d'ordres de tri aléatoires (couleurs et valeurs)
-  Affichage côte à côte : main non triée vs main triée
-  Personnalisation des règles de tri (drag & drop)
-  Design responsive (mobile, tablette, desktop)
-  Cartes stylisées avec symboles Unicode (♥ ♦ ♠ ♣)
-  Gestion d'erreurs avec exceptions personnalisées
-  Commande console pour tests rapides

---
